# 加班排班表 · 使用说明

网页排班 + 飞书自动推送 + DeepSeek 夜间检查 + Prompt 规则 AI 排班 + 管理员工时统计。
**两组独立排班、互不干扰**：每天每组可安排 **0~N 人**出勤（人数要求写在各组规则 md 里），轮换、统计、AI 检查全部按组进行。

## 服务信息

| 项目 | 值 |
|---|---|
| 端口 | `8787`（环境变量 `PORT` 可改） |
| 目录 | `/root/work_overtime` |
| 数据文件 | `data/db.json`（权限 0600，含密码散列与 API Key，不要外传/提交） |
| 一组排班规则 | `prompt_md/prompt.md`（A 组规则） |
| 二组排班规则 | `prompt_md/prompt_b.md`（待编写） |
| 访问地址 | `http://服务器IP:8787`（需在云安全组放行 8787 端口） |

## 启动 / 停止

```bash
systemctl status work-overtime juku    # 查看状态（剧库服务名 juku）
systemctl restart work-overtime        # 重启排班站（git pull 更新代码后执行）
systemctl stop work-overtime           # 停止
journalctl -u work-overtime -n 50      # 看日志
```

两服务均已由 systemd 托管：**开机自启、崩溃 3 秒自动拉起、内存限额**（排班站 200M / 剧库 400M，超限只重启自己，不拖累整机）。

重启不丢数据（排班、日志、登录状态都在 `data/` 里）；重启也不会重复发送或重复调 AI（有补发/补检标记）。

## 登录账号

- 成员由管理员在「设置 → 成员管理」添加，初始密码默认 `123456`（添加时可另行指定），登录后请到「密码」页修改
- 管理员：账号 `admin`，密码 `admin123`（首次部署的初始值，登录后请修改）
- 🔒 **管理员登录入口默认隐藏**：在登录页**连按 6 下 `·`**（中文输入法；英文输入法下同一物理键出的 `` ` `` 也算）后，下拉框才出现「管理员」选项。按其他键计数清零，页面上无任何提示
- 移除成员＝删除账号＋清空其全部排班＋立即下线

## 功能一览

**成员**
- 查看每周排班（每天两组各 0~N 人；自己的班蓝色高亮，「本周」一键跳回）
- 点击日期弹窗操作（只作用于**自己所在组**）：**认领加入**当天名单、**弃班**（从名单移除）、**换班**（两步：选我让出的日期→选对方）
- 任何修改成功后，飞书群自动重发最新排班（防抖 3 秒，避免连续操作刷屏）

**管理员（admin）**
- 📅 排班：点击日期可**按组编辑当天出勤名单**（每天可加/删多人、工时、备注）
- 📊 统计：按组独立统计人均工时（全部/仅已发生/本月）、周均工时、组内公平性提示
- 🪄 AI排班：**按组**编辑排班规则（`prompt.md`/`prompt_b.md`）+ 一键让 DeepSeek 按规则生成排班（详见下文）
- ⚙️ 设置：飞书 webhook、发送时刻、班次时间与工时、**周六双倍开关**、DeepSeek 配置、**成员管理**（加人/减人/换组）
- 📥 批量导入：粘贴 `2026-09-21 张三,李四` 格式（一天可多人，自动落到各成员所在组）；`-` 清空整天
- 📝 日志：所有改动留痕（谁、何时、改了什么）

## 飞书推送

1. 在飞书群里添加「自定义机器人」，拿到 webhook 地址（形如 `https://open.feishu.cn/open-apis/bot/v2/hook/xxx`）；若开启了签名校验，把密钥一并填入
2. 网页「设置」里填入 webhook 地址，保存后**本周排班会立即补发一次**（可用于验证）
3. 之后：每周一 9:00 定时发送本周排班；任何人在网页改排班，自动重发
4. ⚠️ **当前 webhook 指向本机测试用的 mock（`http://127.0.0.1:3999/hook`），上线前请在设置里替换成真实地址**

## 分组排班（两组互不干扰）

- 固定两组：**一组 / 二组**。每个成员属于其中一组；每天每组可安排 **0~N 人**，各自独立
- **正式排班方式＝AI 按各组规则生成**（见下文）。每组可在「成员管理」里开关**自动轮换占位**：
  - 开（默认）：没排班的日子自动按轮换补 1 人保底（含周五/周日）
  - 关：完全由「AI排班」生成或手动指派，**清空排班后保持全空、统计归零**——推荐用 AI 规则排班的组关掉
- 「危险区·清空排班」两个模式：**清空并保持全空**（推荐，默认；顺带自动关闭两组自动轮换占位，统计归零，
  之后排班只来自 AI 生成或手动指派）／**清空并重排占位**（老行为，开轮换的组立即补排 4 周）。
  清空时会**自动忽略所有待处理的 AI 建议**（防止应用到已不存在的旧排班上）
- 认领/弃班/换班只作用于**本组**；管理员按组编辑当天名单；飞书卡片、统计、AI 夜检全部按组口径
- 旧数据升级：原有排班与成员全部自动归入「一组」，之后用「成员管理」把部分人移到二组即可

## 成员管理（管理员 · 设置页）

- **添加成员**：姓名 + 分组 + 代号（空则自动分配 a/b/c…）+ 初始密码（默认 `123456`）
- **改名**：只改「姓名」（登录与界面/飞书显示），排班存的是内部 ID，改名后所有显示自动更新，
  名下有排班的周自动重发飞书，在线会话保持有效（下次登录用新姓名）
- **代号**：规则 md 与 AI 识别用的稳定标识（组内唯一）。规则里写代号（如"主管 a"），
  之后怎么改姓名都不用动规则；AI 上下文里成员显示为「代号（姓名）」，AI 回填代号或姓名都能正确落位
- **移除成员**：账号删除、立即下线、名下全部排班清空，不可恢复
- **调整分组**：清空原组排班，之后按新组轮换
- 姓名全局唯一（登录名就是姓名）；批量导入行里可以写姓名，也可以写代号

## Prompt 规则 AI 排班（管理员 · AI排班页）

让大模型按**各组自己的规则**排班：

1. **排班规则（Markdown · 按组）**：一组存 `prompt_md/prompt.md`，二组存 `prompt_md/prompt_b.md`；
   页面上按组切换显示/编辑。规则里写清楚：成员名单、**每天出勤人数**（如周一 2 人、周六 3 人、周五 0 人）、
   硬性约束（主管绑定、禁排档期、防连续）、优化目标（均贫富、单周上限）
2. **生成排班**：选分组 + 开始日期 + 周数 → 点「🪄 生成排班」→ AI 给出建议（逐日名单 diff）
3. **补充要求输入框**：可选，优先级**高于**规则 md，用于临时微调（如"周三张三请假"）；没有规则文件时，输入框里写的就是规则
4. **应用**：默认需管理员确认；设置里切到「自动应用」则生成后直接生效。应用后飞书自动重发，网页会**自动跳转到受影响的那周**方便核对；
   AI 返回的人员名单对不上成员（比如规则里写了不存在的称呼）会明确报错并忽略该天，不会静默清空；升级前生成的旧格式建议也能正常应用
5. AI 只能安排**目标组成员名单**里的人、只能动**所选日期范围**；规则 md 里写的输出格式（如按星期几的 JSON）
   系统也能解析，无需改动。重排同一周时：**必须覆盖范围内每一天**（AI 输出缺天会报错放弃，绝不半套用，
   避免新旧排班混排）；给 AI 的"均贫富"公平基数**剔除目标周**（被替换的当周旧班次不污染判断）
6. 统计页每次打开都从当前排班**实时计算**，重排/修改/认领后立即反映，无缓存

## 周六双倍

- 设置里可开关（默认开）：周六加班按**双倍工时**计入统计、周均工时与 AI 的"均贫富"历史口径
- 飞书卡片与网页上周六显示的工时即为双倍后的值（如 16h）；管理员单独指定过工时的班次也按双倍放大

## 工作区域 IP 验证（可选功能 · 纯增量）

- **网页按钮**：排班页下方「📍 验证我是否在工作区域」，点一下显示你是否在工作区域及来源 IP
- **配置**（管理员 · 设置页）：工作区域 IP 段支持 CIDR（`192.168.1.0/24`）和单 IP，逗号分隔；
  留空则按钮提示未配置。服务器在反向代理后时，把"真实 IP 来源"切到 X-Forwarded-For（默认直连时忽略该头，防伪造）
- **自动化/MCP 口子**（免登录）：设置一个口令后，任何机器可带请求头调用：
  ```bash
  curl -H "X-Check-Key: 你的口令" http://服务器IP:8787/api/where-am-i
  ```
  返回 JSON：`{"ip":"192.168.1.23","inWorkArea":true,"matched":"192.168.1.0/24",...}`。
  在要验证的那台机器上执行（或让 MCP/agent 用 fetch 调），返回的就是该机器的判定结果
- 配置了 IP 段后，每次验证自动落一条流水（时间 / 姓名 / IP / 是否区域内 / 来源）。管理员在「统计」页可按月份、姓名筛选查看并**导出 CSV**（Excel 直接打开）留档；成员在排班页可展开「📋 我的验证留档」查看自己的最近 20 条。自动化（X-Check-Key）调用同样记录，来源标「自动化」

## 🎬 加班看剧（果果剧库联动 · 可选）

排班站可对接「果果剧库」（红果短剧自建站，Go 单文件）实现：**添加成员自动注册同名短剧账号**、存量成员一键补建、
排班页出现「🎬 加班看剧」入口（新标签页打开剧库）。剧库不可达不影响排班站任何功能。

### 服务器部署剧库（一次性）

1. 把剧库 Linux 程序（`juku_linux_amd64`）上传到服务器（如 `/root/juku/`），`chmod +x juku_linux_amd64`
2. 启动（显式指定管理员账号密码 + 免登录桥密钥；桥密钥与排班站联动设置里的"免登录桥密钥"一致）：
   ```bash
   cd /root/juku
   setsid nohup ./juku_linux_amd64 -listen 0.0.0.0:8999 -open=false \
     -admin-user admin -admin-password '你的10位以上密码' \
     -bridge-secret '随便一串长密钥' -data-dir ./data > juku.log 2>&1 < /dev/null &
   ```
3. 安全组放行 `8999`（建议只对公司出口 IP 放行）；本机验证 `http://127.0.0.1:8999`
4. 服务器只有 1.6G 内存：剧库里设置播放会话上限 3、媒体连接 4、封装缓存 256MB、**视频转码并发 0**；
   建议全员"仅在线观看"（联动建号已默认勾选）

> **部署现状（2026-09-22）**：剧库已由 systemd 托管（服务名 `juku`，开机自启、内存限额 400M、崩溃自动重启），
> 并已开启**直连模式**（`directSources: hongguo`）——兼容内容由成员浏览器直连红果 CDN 拉流，服务器不再中转视频字节，
> 内存/带宽占用大幅下降；加密内容自动回退代理模式。

### 排班站配置（管理员 · 设置页「短剧账号联动」）

1. 填剧库地址（如 `http://服务器IP:8999`）+ 剧库管理员账号密码 + **免登录桥密钥**（与剧库启动参数 `-bridge-secret` 一致）→ 保存（密码/密钥接口永不回显）
2. 点「🔄 同步短剧账号」为存量成员补建 → **生成的密码列表只显示一次**，立即转告成员本人（作为桥失效时的备用登录方式）
3. 勾选"同步时开启必须登录+关闭注册"后，剧库只接受这些账号（外人无法注册进入）
4. 之后每次「成员管理 → 添加成员」自动建同名短剧账号，密码弹窗一次性展示

### 免登录进剧库（成员体验）

- 成员登录排班站后点排班页「🎬 加班看剧」→ **直接以本人身份进入剧库，无需再输密码**
- 原理：排班站用共享密钥现场签发一个 **60 秒有效的签名链接**，剧库验证后直接种下该成员的会话 Cookie——全程不碰、不存任何成员密码，比存密码自动填更安全
- 密钥未配置（任一侧）时退化为打开剧库首页手动登录；成员改名后排班站与剧库账号名不一致时同样退化为手动登录

### 边界（剧库侧无对应接口，属已知限制）

- 剧库账号**不能删除**：移除加班成员后短剧账号残留（只对公司出口可达，离开的人连不上，无实际风险）
- 剧库**无法找回/重置普通账号密码**：成员忘了只能删号重建（记录作废）——发密码时务必提醒改密后记牢
- 剧库用户名要求 **2–32 字**：单字姓名（如 a、b）无法建号，同步结果会明确列出失败原因，改名后重新同步即可
- 加班站改名**不同步**短剧账号名（两边身份独立）

## DeepSeek 夜检

- 每晚 **21:00**（可在设置改）**逐组独立**检查未来 **14 天**排班，检查时会**带上该组的规则 md**
  （规则定义的休息日不会被当成空班去补人）
- **只检查已排班的周**（标记过自动生成、或该周该组已有人被排班）：未排班的空周不提醒、不催排班，
  等你在「AI排班」页生成
- 触发条件：已排班范围内出现无人排班的天，或组内有人班次比别人多 ≥2 班
- 触发后调用 DeepSeek 分析，给出**最小改动**建议；处理方式二选一（设置里改）：
  - `notify`（默认）：建议发送到飞书群 + 网页「AI排班」页，管理员确认后应用
  - `auto`：直接自动应用并重发飞书
- 周一飞书推送：整周都没排班时会明确提示「本周尚未排班」，而不是一排"待认领"
- API Key 存在 `data/db.json`（0600），网页与接口永不回显，只显示掩码

## 换正式加班逻辑

默认按占位规则「每组每天一人、组内循环」自动排班。拿到具体加班逻辑后有两条路：
1. **写进规则 md**：把规则写到 `prompt_md/prompt.md`，用「AI排班」页生成（推荐，改文字就行，不用改代码）
2. **改代码**：改 `lib/scheduler.js` 里的 `nextAssignee(db, beforeDate, groupId)`（谁接下一个班）和
   `ensureWeekGenerated(db, weekStart, groupId)`（一周哪些天需要排班），其余全部不用动

## 已验证（2026-09-18 测试轮）

- 登录/错误密码 401/越权 403/登录限速
- 自动生成轮换排班；换班、弃班、认领及权限边界
- 修改后 3 秒防抖自动重发飞书；webhook 首配自动补发本周
- DeepSeek 真实调用：发现空班 → 生成建议 → 管理员应用 → 自动重发
- 统计口径（全部/已发生/本月）；批量导入（含错误行提示）
- 重启后数据/会话保留，且不重复发送、不重复调 AI

## 已知事项

- 该服务器内存仅 1.6G 且负载较高（同期还跑着 docker/uvicorn 等其他服务，已发生过 OOM）。本服务占用很小（约 70MB），但机器卡顿时网页响应会变慢；长期建议给这台机器减负或加内存
- 服务器未配置 HTTPS；如需域名+证书（反代 443），可以随时加
