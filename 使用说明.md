# 加班排班表 · 使用说明

网页排班 + 飞书自动推送 + DeepSeek 夜间检查 + 管理员工时统计。

## 服务信息

| 项目 | 值 |
|---|---|
| 端口 | `8787`（环境变量 `PORT` 可改） |
| 目录 | `/root/work_overtime` |
| 数据文件 | `data/db.json`（权限 0600，含密码散列与 API Key，不要外传/提交） |
| 访问地址 | `http://服务器IP:8787`（需在云安全组放行 8787 端口） |

## 启动 / 停止

```bash
cd /root/work_overtime
setsid nohup node server.js > server.log 2>&1 < /dev/null &   # 启动（已配开机需求可换 systemd，喊我帮你配）
pkill -f '^node server.js'                                     # 停止
```

重启不丢数据（排班、日志、登录状态都在 `data/` 里）；重启也不会重复发送或重复调 AI（有补发/补检标记）。

## 登录账号（测试阶段初始密码，登录后请到「密码」页修改）

- 成员 **A ~ G**：密码 `123456`
- 管理员：账号 `admin`，密码 `admin123`

## 功能一览

**成员（A~G）**
- 查看每周排班（自己的班蓝色高亮，「本周」一键跳回）
- 点击日期弹窗操作：**换班**（选自己的某天与对方交换）、**弃班**（留空待认领）、**认领**（捡空班）
- 任何修改成功后，飞书群自动重发最新排班（防抖 3 秒，避免连续操作刷屏）

**管理员（admin）**
- 📊 统计：人均工时（全部/仅已发生/本月 三种口径）、周均工时、公平性差距提示——**需管理员密码登录才能看**
- 🤖 AI 建议：查看 DeepSeek 夜检产生的调整建议，一键应用/忽略
- ⚙️ 设置：飞书 webhook、每周一发送时刻（默认 9:00）、班次时间与工时、DeepSeek 配置
- 📥 批量导入：粘贴 `2026-09-21 A` 格式（每天一行），等拿到正式排班表后整表替换最方便
- 📝 日志：所有改动留痕（谁、何时、改了什么）

## 飞书推送

1. 在飞书群里添加「自定义机器人」，拿到 webhook 地址（形如 `https://open.feishu.cn/open-apis/bot/v2/hook/xxx`）；若开启了签名校验，把密钥一并填入
2. 网页「设置」里填入 webhook 地址，保存后**本周排班会立即补发一次**（可用于验证）
3. 之后：每周一 9:00 定时发送本周排班；任何人在网页改排班，自动重发
4. ⚠️ **当前 webhook 指向本机测试用的 mock（`http://127.0.0.1:3999/hook`），上线前请在设置里替换成真实地址**

## DeepSeek 夜检

- 每晚 **21:00**（可在设置改）检查未来 **14 天**排班
- 触发条件：出现空班，或范围内有人班次比别人多 ≥2 班
- 触发后调用 DeepSeek（模型 `deepseek-flash`）分析，给出**最小改动**建议
- 处理方式二选一（设置里改）：
  - `notify`（默认）：建议发送到飞书群 + 网页「AI建议」页，管理员确认后应用
  - `auto`：直接自动应用并重发飞书
- API Key 存在 `data/db.json`（0600），网页与接口永不回显，只显示掩码
- ⚠️ 本次 Key 在聊天里发过明文，测试稳定后建议在 DeepSeek 后台轮换一次

## 换正式加班逻辑

现在按占位规则「一天一人、A→G 循环」自动排班。拿到具体加班逻辑后，只需改
`lib/scheduler.js` 里的 `nextAssignee()`（谁接下一个班）和 `ensureWeekGenerated()`
（一周哪些天需要排班），其余全部不用动。

## 已验证（2026-09-18 测试轮）

- 登录/错误密码 401/越权 403/登录限速
- 自动生成轮换排班；换班、弃班、认领及权限边界
- 修改后 3 秒防抖自动重发飞书；webhook 首配自动补发本周
- DeepSeek 真实调用：发现空班 → 生成建议 → 管理员应用 → 自动重发
- 统计口径（全部/已发生/本月）；批量导入（含错误行提示）
- 重启后数据/会话保留，且不重复发送、不重复调 AI

## 已知事项

- 该服务器内存仅 1.6G 且负载较高（同期还跑着 docker/uvicorn 等其他服务，已发生过 OOM）。本服务占用很小（约 70MB），但机器卡顿时网页响应会变慢；长期建议给这台机器减负或加内存
- 服务器未配置 HTTPS；如需域名+证书（反代 443），可以随时加
